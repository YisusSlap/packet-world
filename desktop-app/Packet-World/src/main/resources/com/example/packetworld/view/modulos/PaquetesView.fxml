<?xml version="1.0" encoding="UTF-8"?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>

<VBox xmlns:fx="http://javafx.com/fxml"
      fx:controller="com.example.packetworld.controller.PaquetesController"
      spacing="15"
      stylesheets="@../../styles/estilos.css"
      style="-fx-padding: 20;">

    <Label text="Inventario Global de Paquetes" style="-fx-font-size: 24px; -fx-font-weight: bold; -fx-text-fill: #89CFF0;"/>

    <HBox alignment="CENTER_LEFT" spacing="15">
        <TextField fx:id="txtBuscar" promptText="🔍 Buscar por contenido, guía o estatus..." prefWidth="350"
                   style="-fx-background-radius: 20; -fx-border-radius: 20;"/>

        <Region HBox.hgrow="ALWAYS" />

        <Label text="* Solo se pueden modificar paquetes en almacén." style="-fx-text-fill: #999; -fx-font-style: italic;"/>

        <Button text="✎ Editar" onAction="#btnEditar" styleClass="btn-secondary"/>
        <Button text="🗑 Eliminar" onAction="#btnEliminar" styleClass="btn-danger"/>
    </HBox>

    <TableView fx:id="tblPaquetes" VBox.vgrow="ALWAYS">
        <columns>
            <TableColumn fx:id="colGuia" text="Guía Envío" prefWidth="130"/>
            <TableColumn fx:id="colDescripcion" text="Contenido" prefWidth="250"/>
            <TableColumn fx:id="colPeso" text="Peso" prefWidth="80"/>
            <TableColumn fx:id="colDimensiones" text="Dimensiones (cm)" prefWidth="150"/>
            <TableColumn fx:id="colEstatus" text="Estatus Actual" prefWidth="120"/>
        </columns>
        <columnResizePolicy>
            <TableView fx:constant="CONSTRAINED_RESIZE_POLICY"/>
        </columnResizePolicy>
    </TableView>

</VBox>